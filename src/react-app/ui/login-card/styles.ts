import type { CSSProperties } from 'react';

export const loginCardPageStyle: CSSProperties = {
  minHeight: '100dvh',
  backgroundColor: 'var(--color-background-body)',
  padding: 'var(--spacing-6)',
};

export const loginCardContentStyle: CSSProperties = {
  width: '100%',
  maxWidth: 400,
};
