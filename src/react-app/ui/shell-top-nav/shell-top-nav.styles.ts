import type { CSSProperties } from 'react';

/** Fixed mega-menu panel widths from the Astryx `shell-top-nav` template. */
export const shellTopNavMegaItemsStyle: CSSProperties = {
  gridColumn: '1 / -1',
  width: 520,
};

export const shellTopNavMegaFeaturedStyle: CSSProperties = {
  width: 240,
};
